import React,{useState,useEffect}from'react';import Availability from './Availability';import{auth}from'./firebase';import{onAuthStateChanged,signInWithEmailAndPassword,signOut}from'firebase/auth';export default function App(){const[user,setUser]=useState(null);const[email,setEmail]=useState('');const[password,setPassword]=useState('');useEffect(()=>onAuthStateChanged(auth,u=>setUser(u||null)),[]);const isAdmin=user?.email===process.env.REACT_APP_ADMIN_EMAIL;if(!user){return(<div className='p-4 max-w-sm mx-auto'><div className='border rounded-2xl p-4'><div className='font-semibold mb-2'>Guesthouse — Sign In</div><input placeholder='email' className='border rounded w-full mb-2 p-2' value={email} onChange={e=>setEmail(e.target.value)}/><input type='password' placeholder='password' className='border rounded w-full mb-2 p-2' value={password} onChange={e=>setPassword(e.target.value)}/><button className='border rounded p-2 w-full' onClick={async()=>{await signInWithEmailAndPassword(auth,email,password)}}>Sign In</button></div></div>)}return(<div className='p-3 max-w-md mx-auto'><div className='flex justify-between mb-2'><div>{user.email}</div><div><span>{isAdmin?'Admin':'Read only'}</span><button className='ml-2 border rounded px-2' onClick={()=>signOut(auth)}>Sign out</button></div></div><Availability isAdmin={isAdmin}/></div>)}